#pragma once


/*
class Force //this class handles force
{
private:
	double forcevec[2];//vector of force, this vector consist of x component, y component 
	double x_force; //x_component of force
	double y_force; //y_component of force
public:
	Force(double x_force = 0, double y_force = 0);//constructor of Force, forcedrct takes parameters 
	void pf();//prints out foredrct
	void cg(bool TorF = true);//takes bool if true->consider gravity, else->doesn't consider
};
*/